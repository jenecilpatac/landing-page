version: '3.8'

services:
  web:
    build: .
    ports:
      - "5001:80"
    volumes:
      - .:/usr/share/nginx/html
    container_name: nginx_html_page2
    networks:
      - webnet

networks:
  webnet:
    driver: bridge
